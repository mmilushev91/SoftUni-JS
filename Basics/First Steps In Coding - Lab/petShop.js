function solve(dogPackages, catPackages) {
  const dogPackagePrice = 2.50;
  const catPackagePrice = 4;
  
  let totalPrice = dogPackages * dogPackagePrice + catPackages * catPackagePrice;
  
  console.log(`${totalPrice} lv.`)
}
