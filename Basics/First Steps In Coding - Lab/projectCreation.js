function solve(name, projectCount) {
  const timePerProject = 3;
  let timeNeeded = timePerProject * projectCount;
  
  console.log(`The architect ${name} will need ${timeNeeded} hours to complete ${projectCount} project/s.`)
}
